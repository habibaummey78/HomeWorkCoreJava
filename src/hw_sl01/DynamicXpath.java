package hw_sl01;

public class DynamicXpath {

	// a. Tag with a single attribute (from Samsung Website)
	// input[@id='searchDomain']

	// b. Tag with multiple attributes with 'and' logic
	// input[@id='eppFlag' and @value='N']

	// c. Tag with inner text
	// label[text()='Enter your search keyword']

	// d. Tag with partial inner text from any website.
	// label[contains(text(), 'Email or')]

	// above all had unique webelements.
}
